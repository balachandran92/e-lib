extends layout

block content
  main
    div#view
      h1.text-center= title
      p.text-center 🙏🏻 தமிழர் களறிக்கு அன்புடன் வரவேற்கிறோம் 🙏🏻
      ul
        each val in folders
          a(href=val.fileName)
            li.thumb
              span(class = val.thumb)
              span(class = 'text-holder')= val.fileName